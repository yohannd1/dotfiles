FILES := main.c
COMPILED_FILES := main.o
COMPILER := gcc

build: clean
	mkdir build
	$(COMPILER) -c $(FILES)
	$(COMPILER) -o build/main $(COMPILED_FILES)
	mv $(COMPILED_FILES) -t build
	
clean:
	if [ -r build ]; then rm -r build; fi

run: build
	./build/main
