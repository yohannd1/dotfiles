#!/usr/bin/env sh

if [ -z "$F_WALLPAPERS" ]; then
  echo "Please set \$F_WALLPAPERS with the wallpapers folder path."
  exit 1
fi

IMAGE="$(shuf -n1 -e "$F_WALLPAPERS"/*.jpg)"
feh --no-fehbg --bg-fill "$IMAGE"
