#!/usr/bin/env sh

if [ $# = 0 ]; then
  printf >&2 "%s\n" "Usage: $(basename $0)"
  exit 1
fi

if [ -z "$F_WALLPAPERS" ]; then
  printf >&2 "Please set %s to the wallpapers folder path\n" '$F_WALLPAPERS'
  exit 1
fi

image="$(shuf -n1 -e "$F_WALLPAPERS"/*.jpg)"
feh --no-fehbg --bg-fill "$image"
