#!/bin/sh

if [ "${1}" = "--help" ] || [ $# != 1 ]; then
    echo "Usage: $(basename "${0}") <file>"
    exit 1
fi

if [ "$(uname -o)" = Android ]; then
   termux-open "${1}"
else
   xdg-open "${1}"
fi
