#!/usr/bin/env sh

if ! type glow >/dev/null 2>/dev/null; then
  printf "'glow' binary not found"
  exit 1
fi

if [ $# = 0 ]; then
  echo "You need to specify at least one argument."
  exit 1
fi

glow -sdark -w $(( $(tput cols) - "${GLOWRAPPER_MARGIN:-2}" )) "$@"
