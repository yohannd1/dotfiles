// vim: ft=jsonc
{
    "layer": "bottom",
    "position": "top",
    "height": 15,
    "spacing": 0,

    "modules-left": ["hyprland/workspaces", "dwl/tags", "hyprland/window", "dwl/window"],
    "modules-right": ["pulseaudio", "clock", "tray"],

    "hyprland/workspaces": {
        "disable-scroll": true,
        "all-outputs": false,
        "warp-on-scroll": false,
        "format": "{name}",
    },

    "tray": {
        // "icon-size": 21,
        "spacing": 5
    },

    "clock": {
        "format": "{:%Y-%m-%d %H:%M}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
    },

    "pulseaudio": {
        // "scroll-step": 1, // %, can be a float
        "format": "vol {volume}%",
        "format-muted": "vol OFF",
    },
}

