#!/usr/bin/env sh

cacheDir="${XDG_CACHE_HOME:-"$HOME/.cache"}"
cacheFile="$cachedir/pathbin"

[ -r "$cacheDir" ] || mkdir -p "$cacheDir"

IFS=:
if stest -dqr -n "$cache" $PATH; then
  stest -flx $PATH | sort -u | tee "$cache"
else
  cat "$cache"
fi
